---
import { Image } from 'astro:assets';
import SectionContainer from '../../ui/SectionContainer.astro';
import porchImage from '../../../assets/images/porch.svg';
import licensedIcon from '../../../assets/icons/licensed.svg';
import premiumIcon from '../../../assets/icons/premium.svg';
import quickIcon from '../../../assets/icons/quick.svg';

interface Feature {
	title: string;
	description: string;
	icon: string;
}

const features: Feature[] = [
	{
		title: 'Licensed and Expertise',
		description: 'Professional installation by certified local experts',
		icon: licensedIcon.src,
	},
	{
		title: 'Premium Fabrics, Built to Last',
		description: 'High-quality materials with comprehensive warranties',
		icon: premiumIcon.src,
	},
	{
		title: 'Quick delivery and Assembly',
		description: 'Our process ensures your awning is ready to enjoy in no time.',
		icon: quickIcon.src,
	},
];
---

<section class="py-12 sm:py-16 md:py-20 lg:py-24 ">
	<SectionContainer>
		<div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center">
			<!-- Left Content -->
			<div class="space-y-8">
				<!-- Headline -->
				<div class="space-y-4">
					<h2 class="scroll-fade-in font-semibold text-[2rem] sm:text-[2.5rem] lg:text-[3rem] leading-[120%] tracking-[-0.02em]">
						Why Choose Us?
					</h2>
					<p class="text-base sm:text-lg leading-[150%] tracking-[-0.002em] text-gray-600">
						Strength you can count on. Beauty you'll love. Backed by two decades of excellence.
					</p>
				</div>

				<!-- Features List -->
				<div class="space-y-6">
					{features.map((feature, index) => (
						<div 
							class="feature-item scroll-fade-in flex items-start gap-4"
							style={`animation-delay: ${index * 100}ms;`}
						>
							<!-- Icon -->
							<div class="flex-shrink-0 flex items-center justify-center bg-gray-100 rounded-xl p-3 transition-transform duration-300 hover:scale-105">
								<img 
									src={feature.icon} 
									alt={feature.title}
									class="w-16 h-16 sm:w-20 sm:h-20"
								/>
							</div>

							<!-- Text Content -->
							<div class="flex-1">
								<h3 class="text-lg sm:text-xl font-semibold leading-[130%] tracking-[-0.01em] text-gray-900 mb-2">
									{feature.title}
								</h3>
								<p class="text-sm sm:text-base leading-[150%] tracking-[-0.002em] text-gray-600">
									{feature.description}
								</p>
							</div>
						</div>
					))}
				</div>
			</div>

			<!-- Right Image -->
			<div class="scroll-fade-in lg:order-last order-first">
				<div class="relative rounded-2xl overflow-hidden shadow-xl">
					<img 
						src={porchImage.src}
						alt="Beautiful outdoor porch with awning"
						class="w-full h-auto object-cover"
					/>
				</div>
			</div>
		</div>
	</SectionContainer>
</section>


